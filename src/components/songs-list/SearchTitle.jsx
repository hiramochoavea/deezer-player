import { useState } from 'react';

const SearchTitle = () => {

    return (
        <div id="search-title-container">
            <p>
                Search results for:
                <b> Adam Beyer</b>
            </p>
        </div>
    );

};

export default SearchTitle;