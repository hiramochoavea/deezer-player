import { useState } from 'react';

const NavBar = () => {

    return (
        <div id="nav-bar-container">
            <nav>
                <a href="#">Home</a>
                <a href="#">Discover</a>
                <a href="#">Recents</a>
                <a href="#">Library</a>  
            </nav>                                  
        </div>
    );

};

export default NavBar;